# Neovim plugin artifacts (managed by lazy.nvim at runtime)
nvim/plugin/

# OS junk
.DS_Store
Thumbs.db
env/.config/tmux/plugins/

# Kitty per-machine override symlink (created by ./deploy from ~/.dotfiles-env)
env/.config/kitty/local.conf
